# 人がログインした Chromium プロファイル（Google の Cookie・トークンが入る）。絶対に commit しない
profile/
profile-*/
# 録音（会議の音声・スクリーンショット・文字起こし）
out/
# 秘密（.env.example だけ残す）
.env
.env.*
!.env.example
# tsx/tsc の一時ファイル
*.tsbuildinfo
# PoC 単体の依存（コンテナ用。手元ではリポジトリ直下の node_modules を使う）
node_modules/
