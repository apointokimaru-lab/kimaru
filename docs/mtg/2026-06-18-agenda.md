# 打ち合わせアジェンダ 2026-06-18（内山さん）

- 日時: 2026-06-18 ／ 出席: 内山さん・塩尻
- ゴール: **本番公開に必要な「外部アカウント作成」と「法務チェック」を片付ける**（＝今残っているのはここだけ）
- 役割: **アカウント作成・契約・法務チェック＝内山さん** ／ **実装・キー投入・デプロイ＝塩尻**
- 進め方: 各項目「やること → 成果物（塩尻へ渡すもの）→ ☑」。前回(6/9)の宿題の進捗確認から入る。

> 記入方法: 終わった項目の `[ ]` を `[x]` に。取得したキー・ID・URLは「→ 」の右にメモ。

---

## ★ 今日のゴール（ここが埋まれば公開できる）

1. **前回宿題（ドメイン・Square・法務）の進捗確認**
2. **公開に必須の外部アカウントを作り、キー類を塩尻へ渡す**（ドメイン / Resend / Square / Google Cloud）
3. **法務3ページを「公開OK」と最終承認**（Google審査の前提にもなる）

> 実装はすべて完了済み（GitHubの実装issueは全クローズ）。**コード側の残作業はゼロ。** 公開を止めているのは下の人間タスクだけ。

---

## 進行（目安40分）

| 時間 | 内容 |
|---|---|
| 5分 | 0. 進捗共有・前回宿題の確認 |
| 15分 | 1. 外部アカウント作成（★本丸） |
| 10分 | 2. 法務チェック（★承認） |
| 5分 | 3. 公開までの段取り・依存関係 |
| 5分 | 4. 確認・決めごと |

---

## 0. 進捗共有・前回(6/9)宿題の確認（5分）

前回 内山さん担当だった3件の進捗を確認する。

- [ ] **ドメイン取得**（`kimaru.jp/.com` は不可・高額 → `kimaru-co.jp` 等の代替を取得予定）→ 取得した？ → ドメイン名:
- [ ] **Square 決済**（本番アカウント・¥980商品）→ どこまで？ →
- [ ] **法務文面の確認**（GitHub `docs/legal/` の3ページ）→ 確認した？ →
- 塩尻側: 実装は全完了。**env（キー）投入とデプロイ待ち**の状態。

---

## 1. 外部アカウント作成（★本丸・15分／内山さん担当）

> 共通アカウント `apointokimaru@gmail.com`（運営Gmail）で各サービスを契約・共同管理する方針。**作成して出てくる「キー・ID」を塩尻へ渡せば、塩尻がNetlifyに入れて公開できる。**

### 1-A. ドメイン（すべての前提）
ドメインが決まらないと、メール・Googleログイン・法務ページURLが全部確定しない。**最優先。**
- [ ] ドメイン取得（候補から1つ確定）→ 確定ドメイン:
- [ ] **Cloudflare** に登録し DNS をCloudflare管理に（無料・後のメール設定/受信転送で使う）
- メモ:

### 1-B. メール送信：Resend（予約確認・リマインダー・通知）
決定13で「独自ドメイン＋Resend Pro」採用。これが無いと**リマインダー等のメールが飛ばない**。
- [ ] **Resend アカウント作成 → Pro（$20/月・5万通）にアップグレード**（無料は100通/日上限で営業ヘビー日に詰まる）
- [ ] 送信専用サブドメインを2つ登録：取引 `notify.<ドメイン>` ／ 営業 `news.<ドメイン>`
- [ ] **SPF / DKIM / DMARC** を Cloudflare DNS に設定（Resendが出すレコードを貼る）
- [ ] **受信**: Cloudflare Email Routing で `info@<ドメイン>` → 既存Gmail へ転送（無料・コード不要）
- 成果物 → 塩尻へ: `RESEND_API_KEY` ／ 送信元アドレス（notify / news）
- メモ:

### 1-C. 決済：Square
- [ ] **Square 本番アカウント作成**
- [ ] サブスク商品 **¥980/月（無料お試しなし・登録＝即課金）** を作成
- [ ] Webhook URL 登録（`https://<ドメイン>/api/square-webhook`）
- [ ] （**プレミアム ¥2,200 はフェーズ2なので今は不要**。¥980会員が50〜100人超えてから。今やるなら商品だけ作って `SQUARE_PREMIUM_PLAN_ID` を控える）
- 成果物 → 塩尻へ: アクセストークン ／ Webhook署名鍵（`SQUARE_WEBHOOK_SHARED_SECRET`）／ プランID
- メモ:

### 1-D. Googleログイン・カレンダー：Google Cloud Console
カレンダーは「制限付きスコープ」→ **本番公開にGoogleのセキュリティ審査（数週間）が必要**。プライバシーポリシー公開URLが前提なので、§2の法務承認と連動。**審査に時間がかかるので早く着手したい。**
- [ ] OAuthクライアント作成（クライアントID／シークレット）
- [ ] OAuth同意画面の設定＋**リダイレクトURI登録**（`https://<ドメイン>/api/google-auth-callback`）
- [ ] **制限付きスコープ（カレンダー）の審査申請**（プライバシーポリシー公開URLが必要）
- 成果物 → 塩尻へ: `GOOGLE_CLIENT_ID` ／ `GOOGLE_CLIENT_SECRET`
- メモ:

### 1-E. （フェーズ2・今すぐ不要）OpenAI／Supabase／Netlify
- [ ] **OpenAI**（AIアシスト=プレミアム用）→ フェーズ2。今は作らなくてよい。
- [ ] **Supabase 本番プロジェクト**：アカウント作成は内山さん、スキーマ適用は塩尻。
- [ ] **Netlify 本番プロジェクト**：所有・課金アカウントの確認（デプロイ作業は塩尻）。
- メモ:

---

## 2. 法務チェック（★承認・10分／内山さん担当）

> 事業者情報は前回 反映済み（**一般社団法人ぴんころ／代表理事 内山 文雄／西本町 fabbit／インボイス T1030005019164／電話 080-6882-9783**）。本文は GitHub `docs/legal/` と公開ページに同期済み。**今日は「公開してよい」の最終承認が要点。**

- [ ] **利用規約・プライバシーポリシー・特商法表記**の文面を確認 → **公開OK**（`/terms.html` `/privacy.html` `/tokushoho.html`）
- [ ] 事業者情報（上記）に**誤り・変更がない**か最終確認（登記と相違なし）
- [ ] **連絡先メール**：当面 `apointokimaru@gmail.com` のままか → ドメイン取得後に独自ドメインのアドレスへ差し替えるか
- [ ] **営業メール（会員獲得サンキュー導線）の文面**を法務チェック：特定電子メール法（送信者表示・配信停止導線）に配慮。※ワンクリック解除（List-Unsubscribe）は実装済み。
- [ ] **プライバシーポリシー公開URLは Google OAuth審査の必須要件** → 公開OKなら §1-D の審査申請に進める
- メモ:

---

## 3. 公開までの段取り・依存関係（5分）

「どれが先か」を共有。**クリティカルパスは ①ドメイン → ②Google審査（数週間）。** これだけ早く回す。

```
① ドメイン取得 ─┬─→ Resend（メール送信元・DNS）
（最優先）       ├─→ Google OAuth リダイレクトURI ─→ 審査申請（数週間・最長）
                ├─→ Square Webhook URL
                └─→ 法務ページ/サービスURLの確定
② 法務「公開OK」─→ プライバシーポリシー公開 ─→ Google審査の前提クリア
③ キー類が揃う ─→ 塩尻が Netlify env 投入 → デプロイ → 本番公開
```

- [ ] 役割確認：**キー取得＝内山さん／env投入・デプロイ＝塩尻**
- [ ] 公開目標日： →
- メモ:

---

## 4. 確認・決めごと（5分）

- [ ] **ドメイン候補の最終決定**（→ 確定：　　　　　）
- [ ] **連絡先メール**：暫定Gmailのまま公開 / 独自ドメインに切り替えてから公開
- [ ] **プレミアム（¥2,200・AI）はフェーズ2で据え置き**でよいか（今回はSquare¥980とOpenAIは作らない）
- [ ] **課金失敗時の運用**（未決定）：カード決済が落ちたら即・無料降格 / 数日猶予して再試行、どちら？（解約条件＝特商法に絡む）
- [ ] その他、内山さん側で詰まっている点・確認したい点
- メモ:

---

## 付録：内山さんが取得して塩尻へ渡すもの（チェックリスト）

| サービス | 渡すもの | 用途（env名） |
|---|---|---|
| ドメイン/Cloudflare | 確定ドメイン名 | `APP_BASE_URL` |
| Resend | APIキー・送信元(notify/news) | `RESEND_API_KEY` / `TRANSACTIONAL_EMAIL_FROM` / `MARKETING_EMAIL_FROM` |
| Square | アクセストークン・署名鍵・プランID | `SQUARE_WEBHOOK_SHARED_SECRET` ほか |
| Google Cloud | クライアントID・シークレット | `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` |
| （後）OpenAI | APIキー | `OPENAI_API_KEY`（フェーズ2） |

> 決まったら `../open-decisions.md` に確定として転記し、`../tasks.md` の👤人間タスクを消し込む。
